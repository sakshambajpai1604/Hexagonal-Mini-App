# Hexagonal Mini-App — Advanced
Objective: Build domain + port interfaces + two adapters, wired by a tiny factory.
1. Define PaymentPort and NotificationPort
2. Implement adapters for each port
3. Write a factory that selects adapters by config
4. Unit-test the domain with mocks

Validation: Domain compiles with zero adapter imports.
